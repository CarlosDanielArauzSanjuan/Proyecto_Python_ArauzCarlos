def calculate_totals_each_category(data, key_category, key_amount): #definir bien los parametros 

    totals = {}
    for item in data:
        category = item[key_category]
        amount = item[key_amount]
        totals[category] = totals.get(category, 0) + amount 
    return totals
