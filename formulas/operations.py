def add(values): 
    return sum(values)


def filter_by_range(data, key, start, end):
    return []
# necesito filtrar los diccionarios por un rango de fechas ... ( iterar)

#se supone debe calcular cada total de cada categoria y los escribe en el diccionario
def calculate_totals_each_category(data, key, amount): #definir bien los parametros 

    totals = {}

#con que ciclo debo iterar para recorrer los datos ?


    return totals